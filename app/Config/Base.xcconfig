// Shared build settings. Local.xcconfig (not in git) can override them.
OASIS_BUNDLE_ID = com.mikeheffler.Oasis
DEVELOPMENT_TEAM =

// Optional include: the build still works when the file does not exist.
#include? "Local.xcconfig"
